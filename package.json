{
  "name": "terrain-3d",
  "version": "1.0.0",
  "description": "Ecological Digital Twin Platform for Landscape Design",
  "scripts": {
    "install:supersplat": "cd supersplat-terrain-3d && npm install",
    "build:supersplat": "cd supersplat-terrain-3d && npm run build",
    "deploy:supersplat": "cp -r supersplat-terrain-3d/dist/* supersplat/",
    "setup:supersplat": "npm run install:supersplat && npm run build:supersplat && npm run deploy:supersplat",
    "dev": "python server.py"
  },
  "dependencies": {
    "puppeteer": "^24.18.0"
  }
}
